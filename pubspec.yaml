name: checksheet
description: "CheckSheet Flutter App"
publish_to: 'none'
version: 16.3.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  excel: ^4.0.6
  shared_preferences: ^2.2.3
  pdfx: ^2.6.0
  path_provider: ^2.1.2
  intl: ^0.19.0
  path: ^1.9.0
  permission_handler: ^11.3.1
  external_path: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
