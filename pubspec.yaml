name: checksheet
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  intl: ^0.19.0
  flutter:
    sdk: flutter
  excel: ^4.0.6
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  permission_handler: ^11.3.1
  path: ^1.9.0
  file_picker: ^8.1.7
  mobile_scanner: ^5.1.0
  # Syncfusion PDF 뷰어 (독립 줌 컨트롤 및 의존성 완벽 호환)
  syncfusion_flutter_pdfviewer: ^26.2.1

dev_dependencies:
  intl: ^0.19.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "checksheet.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

