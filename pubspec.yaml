name: checksheet_final
description: "CheckSheet Flutter App"
publish_to: 'none'
version: 16.3.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  excel: ^4.0.6
  file_picker: ^8.0.5
  shared_preferences: ^2.2.3
  flutter_pdfview: ^1.3.2  # ❗ 안정성이 검증된 뷰어로 회귀
  path_provider: ^2.1.2
  intl: ^0.19.0
  path: ^1.9.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
