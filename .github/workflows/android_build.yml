name: Flutter Android Build
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.41.7'

      # ❗ 오직 'pdf_test_app' 프로젝트만 빌드
      - name: Build PDF Test App
        run: |
          cd pdf_test_app
          flutter clean
          flutter pub get
          flutter build apk --release
        
      - name: Upload Test APK
        uses: actions/upload-artifact@v4
        with:
          name: PDF-Viewer-Test-APK
          path: pdf_test_app/build/app/outputs/flutter-apk/app-release.apk
