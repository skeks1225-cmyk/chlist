name: pdf_pdfrx_test
description: "A new Flutter project for testing pdfrx."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ❗ 최신 버전(2.x)으로 제한을 풉니다.
  pdfrx: ^2.2.24
  file_picker: ^8.1.7
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
